{% extends "FTFSUserBundle::layout.html.twig" %}

{% block navbar_session %}
{% endblock %}

{% block navbar_menu %}
{% endblock %}

{% block body %}
<div class="row-fluid">
    <div class="span3"></div>
    <div class="span6 well">
        <p>{{ 'registration.confirmed'|trans({'%username%': user.username}, 'FOSUserBundle') }}</p>
        <p>Please login by clicking the following link <br> <a href="{{ path('fos_user_security_login') }}">login</a></p>
{#
        {% if app.session is not empty %}
            {% set targetUrl = app.session.get('_security.target_path') %}
            {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans({}, 'FOSUserBundle') }}</a></p>{% endif %}
        {% endif %}
#}
    </div>
    <div class="span3"></div>
</div>
{% endblock %}
