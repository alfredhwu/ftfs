{% extends 'FTFSCrudBundle:Crud:show_content.html.twig' %}

{% block crud_actions_icons %}
    {% if entity.status in ['20_unsent', '10_rejected'] %}
        {% if removable is defined and removable %}
        <a class="btn btn-small" href="{{ path(prefix~'_delete', { 'id' : entity.id }) }}" rel="tooltip" title="{{ 'ftfs.crud.form.action.show.delete.tooltip'|trans({}, 'crud') }}" data-placement="bottom">
            <i class="icon-trash"></i>
        </a>
        {% endif %}
        <a class="btn btn-small" href="{{ path(prefix~'_edit', { 'id' : entity.id }) }}" rel="tooltip" title="{{ 'ftfs.crud.form.action.show.modify.tooltip'|trans({}, 'crud') }}" data-placement="bottom">
            <i class="icon-pencil"></i>
        </a>
        {% if entity.status == '20_unsent' %}
        <a class="btn btn-small" href="{{ path(prefix~'_send', { 'id' : entity.id }) }}" rel="tooltip" title="{{ (prefix~'_form_action_show_send_tooltip')|replace('_', '.')|trans({}, 'crud') }}" data-placement="bottom">
            <i class="icon-envelope"></i>
        </a>
        {% endif %}
    {% endif %}
    <a class="btn btn-small" href="{{ path(prefix~'_index') }}" rel="tooltip" title="{{ 'ftfs.crud.form.action.show.list.tooltip'|trans({}, 'crud') }}" data-placement="left">
        <i class="icon-list"></i>
    </a>
{% endblock %}

{% block crud_actions %}
    <a href="{{ path(prefix~'_index') }}" class="btn btn-primary">
        <i class="icon-list icon-white"></i>
        {{ 'ftfs.crud.form.action.show.list'|trans({}, 'crud') }}
    </a>
    {% if entity.status in ['20_unsent', '10_rejected'] %}
        <a href="{{ path(prefix~'_edit', { 'id' : entity.id }) }}" class="btn">
            <i class="icon-pencil"></i>
            {{ 'ftfs.crud.form.action.show.modify'|trans({}, 'crud') }}
        </a>
        {% if entity.status == '20_unsent' %}
        <a href="{{ path(prefix~'_send', { 'id' : entity.id }) }}" class="btn">
            <i class="icon-envelope"></i>
            {{ (prefix~'_form_action_show_send')|replace('_', '.')|trans({}, 'crud') }}
        </a>
        {% endif %}
    {% endif %}
{% endblock %}
