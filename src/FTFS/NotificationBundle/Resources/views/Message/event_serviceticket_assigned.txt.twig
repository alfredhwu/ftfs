{# from agent #}
{% if actor.isAgent() %}
    {# to himself #}
    {% if actor == destinaire %}
    This is to confirm you that you have taken the ticket (Reference: "{{ subject.name }}", Client Reference Number: "{{ subject.referenceClient }}") of {{ subject.requestedBy }}.
    {# to ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    This is to inform you that we have assigned your ticket (Reference: "{{ subject.name }}", Client Reference Number: "{{ subject.referenceClient }}").
    {# to other agent #}
    {# to others #}
    {% else %}
    {{ actor }} has taken the ticket (Reference: "{{ subject.name }}", Client Reference Number: "{{ subject.referenceClient }}") at {{ acted_at }}.
    {% endif %}
{# from client #}
{% else %}
    {# to himself #}
    {# to ticket owner #}
    {# to other agent #}
    {# to others #}
{% endif %}
    You can review and modify this ticket in our system by the reference no. "{{ subject.name }}".
