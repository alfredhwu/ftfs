{# event sent from an action of a client #}
{% if 'ROLE_CLIENT' in actor.roles %}
    We are very sorry but this is a spam message which normally you should never receive. Please contact our agents if you keep receiving this email.
    spam code: 7614
{# event sent from an action of an agent #}
{% else %}
    {# confirmation of action for a agent to himself #}
    {% if actor == destinaire %}
        {% if actor == subject.assignedTo %}
        This message is to confirm you that you have successfully taken a service ticket named "{{ subject.name }}".
        {% else %}
        This message is to confirm you that you have successfully assign a service ticket named "{{ subject.name }}" to {{ subject.assignedTo }}.
        {% endif %}
    {# information of action for ther service ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    We are very sorry but this is a spam message which normally you should never receive. Please contact our agents if you keep receiving this email.
    spam code: 5498
    {# information of action for other observator #}
    {% else %}
        {% if actor == subject.assignedTo %}
        This message is to inform you that {{ actor }} has taken the service ticket named "{{ subject.name }}" of {{ subject.requestedBy }} at {{ acted_at }}.
        {% else %}
        This message is to inform you that {{ actor }} has assigned to {{ subject.assignedTo }}the service ticket named "{{ subject.name }}" of {{ subject.requestedBy }} at {{ acted_at }}.
        {% endif %}
    {% endif %}
{% endif %}
{% include "FTFSNotificationBundle:Message:message_service_ticket_info.txt.twig" %}
