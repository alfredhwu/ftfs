{# from agent #}
{% if actor.isAgent() %}
    {# to himself #}
    {% if actor == destinaire %}
    This is to confirm you that you have pended the ticket (Reference: "{{ subject.name }}", Client Reference Number: "{{ subject.referenceClient }}") of {{ subject.requestedBy }}. {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}
    {# to ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    This is to inform you that we have pended your ticket (Reference: "{{ subject.name }}", Client Reference Number: "{{ subject.referenceClient }}"). {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}
    {# to other agent #}
    {# to others #}
    {% else %}
    {{ actor }} has pended the pending ticket (Reference: "{{ subject.name }}", Client Reference Number: "{{ subject.referenceClient }}") at {{ acted_at }}. {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}
    {% endif %}
{# from client #}
{% else %}
{% endif %}
    You can review and modify this ticket in our system by the reference no. "{{ subject.name }}".
