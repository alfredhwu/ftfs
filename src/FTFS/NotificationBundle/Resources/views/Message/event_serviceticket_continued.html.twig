{# from agent #}
{% if actor.isAgent() %}
    {# to himself #}
    {% if actor == destinaire %}
    <p>This is to confirm you that you have continued the ticket (Reference: <a class="btn btn-min" href="{{ subject_href }}">"{{ subject.name }}"</a>, Client Reference Number: "{{ subject.referenceClient }}") of {{ subject.requestedBy }}. {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}</p>
    {# to ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    <p>This is to inform you that we have continued your ticket (Reference: <a class="btn btn-min" href="{{ subject_href }}">"{{ subject.name }}"</a>, Client Reference Number: "{{ subject.referenceClient }}"). {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}</p>
    {# to other agent #}
    {# to others #}
    {% else %}
    <p>{{ actor }} has continued the pending ticket (Reference: <a class="btn btn-min" href="{{ subject_href }}">"{{ subject.name }}"</a>, Client Reference Number: "{{ subject.referenceClient }}") at {{ acted_at }}. {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}</p>
    {% endif %}
{# from client #}
{% else %}
    {# to himself #}
    {% if actor == destinaire %}
    <p>This is to confirm you that you have continued the ticket (Reference: <a class="btn btn-min" href="{{ subject_href }}">"{{ subject.name }}"</a>, Client Reference Number: "{{ subject.referenceClient }}"). {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}</p>
    {# to ticket owner #}
    {% elseif destinaire == subject.assignedTo %}
    <p>{{ actor }} has continued the service ticket (Reference: <a class="btn btn-min" href="{{ subject_href }}">"{{ subject.name }}"</a>, Client Reference Number: "{{ subject.referenceClient }}") at {{ acted_at }}. Please examin this request as soon as possible. {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}</p>
    {# to other agent #}
    {# to others #}
    {% else %}
    <p>{{ actor }} has continued the pending ticket (Reference: <a class="btn btn-min" href="{{ subject_href }}">"{{ subject.name }}"</a>, Client Reference Number: "{{ subject.referenceClient }}") at {{ acted_at }}. {% include 'FTFSNotificationBundle:Message:message_observation_rendering.txt.twig' %}</p>
    {% endif %}
{% endif %}
    <p>You can review and modify this ticket in our system by the reference no. <a class="btn btn-min" href="{{ subject_href }}">"{{ subject.name }}"</a>. </p>
