{# event sent from an action of a client #}
{% if 'ROLE_CLIENT' in actor.roles %}
    We are very sorry but this is a spam message which normally you should never receive. Please contact our agents if you keep receiving this email.
    (spam code: 8184)
{# event sent from an action of an agent #}
{% else %}
    {# confirmation of action for a agent to himself #}
    {% if actor == destinaire %}
    This message is to confirm you that you have successfully closed the service ticket "{{ subject.name }}" of our client {{ subject.requestedBy }}.
    {# information of action for ther service ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    This message is to confirm you that we have closed your service ticket: "{{ subject.name }}".
    If you have further problems concerning this ticket, please contact our agent by writing to support@fujitsu-telecom.fr or call the 01.xx.xx.xx .
    {# information of action for other observator #}
    {% else %}
    {{ actor }} has closed the service ticket "{{ subject.name }}" of {{ subject.requestedBy }} at {{ acted_at }}.
    {% endif %}
    You can still review this ticket in our system by the no. "{{ subject.name }}"
{% endif %}
