{# from agent #}
{% if actor.isAgent() %}
    {# to himself #}
    {% if actor == destinaire %}
    This is to confirm you that you have closed the ticket (Reference: "{{ subject.name }}") of {{ subject.requestedBy }}.
    {# to ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    This is to inform you that we have closed your ticket (Reference: "{{ subject.name }}").
    {# to other agent #}
    {# to others #}
    {% else %}
    {{ actor }} has closed the ticket (Reference: "{{ subject.name }}") at {{ acted_at }}.
    {% endif %}
{# from client #}
{% else %}
{% endif %}
    You can review and modify this ticket in our system by the reference no. "{{ subject.name }}".
