{# from agent #}
{% if actor.isAgent() %}
    {# to himself #}
    {% if destinaire == actor %}
    This is to confirm you that you have left an investigation report.
    {# to ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    This is to inform you that we have finish a session of investigation and left an investigation report concerning your service ticket.
    {# to other agent #}
    {# to others #}
    {% else %}
    {{ actor }} has finished a session of investigation and left an investigation report.
    {% endif %}
{# from client #}
{% endif %}
    You can review and modify this ticket in our system by the reference no. "{{ subject.name }}".
