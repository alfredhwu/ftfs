{% if 'event.serviceticket.show' == action %}
    {% if 'system' == method %}
    {% elseif 'email' == method %}
    {% elseif 'sms' == method %}
    {% else %}
    {% endif %}
{% elseif 'event.serviceticket.update' == action %}
    {% if 'system' == method %}
        {# 'event.serviceticket.update.system.txt' | trans({
            '%service_ticket%' => $service_ticket->getName(),
            '%visited_by%' => $eventlog->getActor(),
            '%visited_at%' => $eventlog->getActedAt()->format('Y-m-d H:i:s'),
            '%change_set%' => $change_set,
            }, 'notification'}) #}
        {{ visited_at }}: "{{ service_ticket }}" has been modified by {{ visited_by }}.
    {% elseif method in ['email', 'sms'] %}
        This message is to inform you that your ticket of number "{{ service_ticket }}" has been modified by {{ visited_by }} at {{ visited_at }}.
        {% for fieldname, fieldchange in change_set %}
            "{{ fieldname }}" : from "{{ fieldchange[0] }}" to "{{ fieldchange[1] }}"
        {% endfor %}
    {% else %}
        Event {{ event_key }}, by {{ acted_by }} at {{ acted_at }}.
    {% endif %}
{% endif %}
