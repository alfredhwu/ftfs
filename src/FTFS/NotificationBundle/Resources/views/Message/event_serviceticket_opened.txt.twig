{# from agent #}
{% if actor.isAgent() %}
    {# to himself #}
    {% if actor == destinaire %}
    This is to confirm you that you have opened a ticket (Reference: "{{ subject.name }}") for {{ subject.requestedBy }}.
    {# to ticket owner #}
    {% elseif destinaire == subject.requestedBy %}
    This is to inform you that we have opened a ticket (Reference: "{{ subject.name }}") for you as demanded.
    {# to other agent #}
    {# to others #}
    {% else %}
    {{ actor }} has opened a ticket for {{ subject.requestedBy }} (Reference: "{{ subject.name }}") at {{ acted_at }}.
    {% endif %}
{# from client #}
{% else %}
{% endif %}
    You can review and modify this ticket in our system by the reference no. "{{ subject.name }}".
